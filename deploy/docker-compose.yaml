services:
  nodejs-app:
    image: "${IMAGE_REPOSITORY}:${IMAGE_TAG}"
    container_name: nodejs-aws-jenkins 

    restart: unless-stopped

    ports:
      - "3000:3000"

    environment:
      NODE_ENV: production

    security_opt:
      - no-new-privileges:true

    read_only: true

    tmpfs:
      - /tmp:size=64m,mode=1777

    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://127.0.0.1:3000',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))"
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"